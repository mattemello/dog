#include <stdlib.h>
#include <stdio.h>
#include "dogFunc.h"

int main(int argc, char** argv) {

    if(argc < 1) {
        printf("ERROR - not enough argument in the command line\n");
        exit(EXIT_FAILURE);
    }

    return 0;
}
